{{!-- <li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}WAITING TO BE EATEN{{/if}}
	</button>

    <button class="devour-burger" data-id="{{this.id}}">DEVOURED!</button>

</li> --}}